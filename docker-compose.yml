services:
  postgres:
    build:
      context: .
      dockerfile: ./dockerfiles/postgres.Dockerfile
    container_name: impostor_database
    restart: always
    env_file: .env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/init.sql

  backend:
    build:
      context: ./backend
      dockerfile: ../dockerfiles/backend.Dockerfile
    container_name: impostor_backend
    restart: always
    env_file: .env
    depends_on:
      - postgres

  frontend:
    build: 
      context: .
      dockerfile: ./dockerfiles/frontend.Dockerfile
    container_name: impostor_frontend
    restart: always
    env_file: .env
    environment:
      - HOST_BACKEND=${HOST_BACKEND}
      - HOST_BACKEND_API=${HOST_BACKEND_API}
      - HOST_FRONT=${HOST_FRONT}
    volumes:
      - ./nginx/frontend.conf:/etc/nginx/conf.d/default.conf:ro

  nginx:
    image: nginx:alpine
    container_name: nginx_proxy
    ports:
      - "${NGINX_PORT}:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data: